I don't use build or build:watch but they came with the boilerplate and I'll keep them for later. The code they compile isn't compatible with all browsers. 

Use "npm run serve" to develop with hot-reload. 
